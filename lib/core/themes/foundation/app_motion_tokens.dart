import 'app_motion.dart';

typedef AppDurations = AppMotion;

abstract final class MotionDurations {
  static const Duration snackbar = Duration(seconds: 3);
  static const Duration tip = Duration(seconds: 5);
}
